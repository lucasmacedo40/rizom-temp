# Mosquitto — produção
listener 1883 0.0.0.0

# Sem autenticação por enquanto (ESP32 acessa direto)
# Para habilitar auth: configure pwfile e comente allow_anonymous
allow_anonymous true

# Persistência
persistence true
persistence_location /mosquitto/data/

log_type error
log_type warning
